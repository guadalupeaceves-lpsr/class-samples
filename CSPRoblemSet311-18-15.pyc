# This allows the creation of something.
import random
# This will assign random_number to a number chosen by the program in between 1 and 5, 
# which will make it into an integer variable.
random_number = random.randint(1,6)
print(random_number)
# This will print, "I'm thinking of a number between 1 and 5. Enter your guess!"
print("I'm thinking of a number between 1 and 5. Enter your guess!")
# This will make a variable called guessed_number, and will assign it to the value of whatever
# was typed in by the user. This would ultimately make it into an integer variable.
guessed_number = raw_input()
# I created a list variable, and in the list there is no items.

# This is a while statement, and 
while guessed_number != random_number:
	if guessed_number > random_number:
		print("The number that you have guessed is too high. Try again.")
		guessed_input = raw_input()
elif guessed_number < random_number:
		print("The number that you have guessed is too low. Try again.")
		guessed_number = raw_input()
else:
	print("You have guessed correctly. Good job! I wonder if you cheated...")
